{
  "name": "fastapi-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Python Workers MCP Demo",
  "scripts": {
    "build": "./build.sh",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}